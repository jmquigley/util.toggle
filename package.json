{
  "name": "util.toggle",
  "version": "0.0.5",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Functions used to toggle strings on/off within a Set",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.toggle"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.toggle/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "util.pkgwrap": "0.0.67"
  },
  "devDependencies": {
    "@types/node": "^8.0.19",
    "ava": "^0.21.0",
    "coveralls": "^2.13.1",
    "nyc": "^11.1.0",
    "tslint": "^5.5.0",
    "typescript": "^2.4.2"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "all": "yarn install && yarn run clean && yarn run lint && yarn run build && yarn test && yarn run docs && yarn run report",
    "build": "pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "docs": "pkgwrap --docs",
    "lint": "pkgwrap --lint",
    "postinstall": "pkgwrap --postinstall",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
